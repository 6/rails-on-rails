# Use the README file as a reference to customize settings.

Authentication:
  order_by: -created_at
  filters: created_at
  search: uid
  fields:
    default: user_id, provider, uid, created_at
  application: Application
